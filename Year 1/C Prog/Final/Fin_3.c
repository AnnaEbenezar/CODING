#include <stdio.h>

/*
3. (6 points total) Given the following function skeleton:

____ sum_even(____ a, ____ n)
{
    // ...
}
3.1) (3 points) Fill in the missing return type and argument 
list, then write the function so that it computes the sum of 
even numbers in the array pointed to by a with size n using 
array subscript operator [].

3.2) (3 points) Complete the function using pointer arithmetic 
(*, ++, --) instead.

// example use of the function
int arr[6] = {1, 2, 3, -4, 5, 6};
// "result" should be 4
int result = sum_even(arr, 6);
*/

void sum_even()